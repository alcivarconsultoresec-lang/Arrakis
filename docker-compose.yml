version: "3.9"
services:
  jarbis:
    build: .
    container_name: omega-jarbis
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./:/app
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
